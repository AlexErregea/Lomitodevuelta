{
  "// nota": "Import map de las Edge Functions (Deno). Mapea los paquetes del dominio para reusar EL MISMO codigo de matching en la capa 3 (matching-engine.md §2). @lomito/matching es TS puro sin imports de runtime externos; @lomito/shared y zod se mapean por si el grafo los toca.",
  "imports": {
    "@lomito/matching": "../../packages/matching/src/index.ts",
    "@lomito/shared": "../../packages/shared/src/index.ts",
    "zod": "npm:zod@^3.24.0"
  }
}
